// Componente de prompt de instalação PWA
// Pode ser expandido para mostrar prompt de instalação do app

const PWAInstallPrompt = () => {
  // Por enquanto, não renderiza nada visível
  // Pode ser implementado posteriormente para prompt de instalação PWA
  return null;
};

export default PWAInstallPrompt;

